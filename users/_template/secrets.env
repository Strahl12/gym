# Per-user secrets. Copy to users/<your-name>/secrets.env and fill in.
# Shared ANTHROPIC_API_KEY lives in the root secrets.env (or shell env).

# Hevy API key — get at https://hevy.com/settings?developer
HEVY_API_KEY=

# Withings (optional — bodyweight tracking)
# Register an app at https://developer.withings.com, then run:
#   python run.py --user <name> --withings-auth
# to complete the OAuth dance. Refresh token will be cached under
# users/<name>/withings_token.json — no need to set the token vars manually.
WITHINGS_CLIENT_ID=
WITHINGS_CLIENT_SECRET=
WITHINGS_REFRESH_TOKEN=

# Optional — port for the local OAuth callback listener (default: 8765)
# WITHINGS_OAUTH_PORT=8765

# Optional — phone number or Apple ID to receive iMessage notifications.
# macOS only; no-op on Pi. Leave blank to disable.
IMESSAGE_RECIPIENT=
