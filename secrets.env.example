# Root secrets.env — SHARED across all users. Copy to `secrets.env` and fill in.
# Per-user keys (HEVY_API_KEY, WITHINGS_*) live in users/<name>/secrets.env.

# Shared Claude key — one key billed to whoever runs the Pi.
ANTHROPIC_API_KEY=
